{
  "name": "pico-nhl-server",
  "version": "0.0.1",
  "description": "Server to proxy the unofficial NHL api for usage in a personal project",
  "license": "MIT",
  "author": "",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "build": "tsc",
    "run": "node dist/index.js",
    "dev": "tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
